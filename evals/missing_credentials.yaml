name: abnormal_remediation_manage returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: abnormal_remediation_manage
  args: {}
  expect:
    is_error: true
timeout: 30s
